exports.run = (bot, message, args) => {
    message.channel.sendMessage(`*This command is still being worked on. You'll be able to delete commands permanently, directly over discord! You'll need to provide a password, though!*`);
}