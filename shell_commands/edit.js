exports.run = (bot, message, args) => {
    message.channel.sendMessage(`*This command is still being worked on. You'll be able to edit commands by downloading a copy, editing that copy, re-uploading that copy, and then commit your copy to the main program, directly over discord! Like GitHub inside Discord!*`);
}